# QR Code Generator

A clean, modern, fully client-side QR code generator that turns any URL into a QR code you can download as a PNG.  
No backend, no tracking, no data storage — everything runs in your browser.

## 🚀 Live Demo
https://YOUR_USERNAME.github.io/qr-generator/

## ✨ Features
- Generate QR codes from URLs instantly
- Automatic URL normalization (`example.com` → `https://example.com`)
- Basic URL validation
- Smooth placeholder → QR transition (animated swap)
- Download generated QR as PNG
- Responsive, modern UI (works great on mobile and desktop)
- 100% client-side (no server, no database)

## 🛠️ Tech Stack
- HTML5
- CSS3
- Vanilla JavaScript
- [qrcodejs](https://github.com/davidshimjs/qrcodejs)

## 📦 Run Locally
1. Clone the repo:
   ```bash
   git clone https://github.com/YOUR_USERNAME/qr-generator.git


2. Open the project folder and launch `index.html` in your browser.

No build tools or dependencies required.

## 🌐 Deploy to GitHub Pages

1. Push the project to the `main` branch
2. Go to **Repository Settings → Pages**
3. Under **Build and deployment**, select:

   * **Source:** Deploy from a branch
   * **Branch:** `main` (root)
4. Save and open the generated GitHub Pages URL

## 🔐 Security & Privacy

* No cookies, analytics, or trackers
* No data is stored or sent anywhere
* URLs never leave the browser
* The app only encodes the provided text into a QR code (it does not visit or execute the URL)

⚠️ Note: The tool does not check whether a URL is malicious. It simply converts the provided URL into a QR code.

## 📄 License

MIT License

```
```
